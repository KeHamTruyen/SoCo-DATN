# Continuous Integration: lint, test, build per package.
# Requires: DATABASE_URL present for Prisma validate/generate (dummy URL is enough).

name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DATABASE_URL: postgresql://ci:ci@127.0.0.1:5432/ci

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build

  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: ci
          POSTGRES_PASSWORD: ci
          POSTGRES_DB: ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ci -d ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            database/package-lock.json
      - name: Install Prisma package
        working-directory: database
        run: npm ci
      - name: Install backend
        working-directory: backend
        run: npm ci
      - name: Validate Prisma schema
        working-directory: database
        run: npx prisma validate
      - name: Generate Prisma client (writes to backend/node_modules)
        working-directory: database
        run: npm run generate
      - name: Apply migrations (tests import modules that open a DB connection)
        working-directory: database
        run: npx prisma migrate deploy
      - name: Seed database (QA users & products for integration tests)
        working-directory: database
        env:
          SEED_ADMIN_EMAIL: ci-seed-admin@soco.local
          SEED_ADMIN_PASSWORD: CiSeedAdmin123!
          SEED_ADMIN_USERNAME: ci_seed_admin
          SEED_QA_USER_PASSWORD: QaUser@123
        run: npx prisma db seed
      - name: Test backend (unit, HTTP smoke, integration)
        working-directory: backend
        env:
          JWT_SECRET: ci-jwt-secret-for-integration-tests-min-32-chars
        run: npm run test:all

  admin-frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: admin/frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: admin/frontend/package-lock.json
      - run: npm ci
      - run: npm run build

  admin-backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: ci
          POSTGRES_PASSWORD: ci
          POSTGRES_DB: ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ci -d ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://ci:ci@127.0.0.1:5432/ci
      ADMIN_JWT_SECRET: ci-admin-jwt-secret-min-32-chars-ok
      ADMIN_PORT: "5001"
      SEED_DEMO_ADMIN_PASSWORD: DemoAdmin@123
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: |
            admin/backend/package-lock.json
            admin/shared/package-lock.json
            backend/package-lock.json
            database/package-lock.json
      - name: Install Prisma package
        working-directory: database
        run: npm ci
      - name: Install backend (Prisma client output target)
        working-directory: backend
        run: npm ci
      - name: Generate Prisma client for admin API
        working-directory: database
        run: npm run generate
      - name: Apply migrations
        working-directory: database
        run: npx prisma migrate deploy
      - name: Seed database (includes demo.admin)
        working-directory: database
        env:
          SEED_ADMIN_EMAIL: ci-seed-admin@soco.local
          SEED_ADMIN_PASSWORD: CiSeedAdmin123!
          SEED_ADMIN_USERNAME: ci_seed_admin
          SEED_QA_USER_PASSWORD: QaUser@123
          SEED_DEMO_ADMIN_PASSWORD: DemoAdmin@123
        run: npx prisma db seed
      - name: Install admin shared
        working-directory: admin/shared
        run: npm ci
      - name: Ability selfcheck
        working-directory: admin/shared
        run: node ability.selfcheck.js
      - name: Install admin API
        working-directory: admin/backend
        run: npm ci
      - name: Start admin API
        working-directory: admin/backend
        run: |
          node src/server.js &
          for i in $(seq 1 30); do
            if curl -sf http://127.0.0.1:5001/health >/dev/null; then
              echo "admin API up"
              exit 0
            fi
            sleep 1
          done
          echo "admin API failed to start"
          exit 1
      - name: Smoke authz (login / 401 user-token / 403 demo)
        working-directory: admin/backend
        run: npm run smoke
