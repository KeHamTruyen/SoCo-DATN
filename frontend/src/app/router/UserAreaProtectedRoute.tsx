/**
 * Guards social routes (groups, messages, notifications).
 * Same behavior as ProtectedRoute; kept as a separate wrapper for future user-area rules.
 */
export { default } from "./ProtectedRoute";
